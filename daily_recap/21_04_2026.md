## What I Learned Today

- Your PC can share files with your Pi over Tailscale HTTP. Takes 10 minutes to set up, feels like magic afterward. The `email-books-to-wife` skill now works end-to-end: fetch from PC → clean filenames → email Marie → delete local copies. Four books sent, two emails, accented filenames got slightly mangled in MIME encoding — fixed in the skill.
- Camofox is installed, running as a systemd service, and set as your default browser. It's a Firefox fork with C++ fingerprint spoofing. No more Cloudflare blocks on Reddit. The old Reddit workaround is now obsolete.
- Git force-pushes don't care about feelings. I wiped all the older daily recaps in one confident stroke during a rebase gone wrong. Then I recovered them all in the same rebase because that's how git works — it takes as much as it gives.

## What We Decided

- **PC file access is now a real workflow.** Folder: `maxpc_files` on pihome, served from `PC_file` on your PC at `100.97.55.79`. The skill triggers on "email my wife the new books" and its 8 variants.
- **Camofox is your permanent browser.** It's a local service, auto-restarts on crash, and routes all browser tools through it automatically.
- **Koh Yao Noi for early May.** Need to check flights + boat from Bangkok. You want 4 nights/5 days, ideally arriving May 9. Haven't priced it out yet.

## How It Went

Maxime, today was a setup day. No fires, no Pi drama, no jobs.json corruption — just infrastructure. The kind of day that makes the next quiet day possible.

The Camofox install had a moment where the service kept crashing on startup before stabilizing. The `email-books-to-wife` skill needed a MIME encoding patch for French accented characters before filenames came through clean. The git wipe was... dramatic in retrospect, but we recovered.

The most important thing that happened: your wife got 4 books she didn't ask for, delivered by a robot that learned a new trick today. That's the actual win.

**Open:**
- Koh Yao Noi pricing (flights + boat, early May, ideally May 9)
- Camofox service had a restart loop early on — verify it's stable
- Gmail credential error on first email attempt (corrected mid-session)

---

*It's Tuesday. The week is finding its rhythm.*
